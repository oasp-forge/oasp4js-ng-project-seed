# oasp4js-ng-project-seed
Seed for AngularJS 1.x projects. Supports JavaScript / ES2015 / TypeScript

+ Prerequisites
    - node.js 5.0.0
    - `npm install -g bower`
    - `npm install -g gulp`
+ `npm install`
+ `typings install`

